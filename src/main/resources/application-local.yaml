# Local development profile - auth service runs at localhost:8081
# Activate with: spring.profiles.active=local
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8081

  cloud:
    gcp:
      project-id: "${MCART_GCP_PROJECT_ID:${GOOGLE_CLOUD_PROJECT:my-project-chat-ai-487408}}"
      credentials:
        location: ${GCP_CREDENTIALS_PATH:}
      pubsub:
        enabled: ${GCP_PUBSUB_ENABLED:true}

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5433/user}
user:
  pubsub:
    enabled: ${USER_PUBSUB_ENABLED:true}

